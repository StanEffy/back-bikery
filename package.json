{
  "name": "back-bikery",
  "version": "1.0.0",
  "description": "backend service written in node, express ",
  "main": "app.js",
  "scripts": {
    "start": "pm2 start server.js",
    "start:prod": "NODE_ENV=production nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "StanEfimov",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.1",
    "express": "^4.18.1",
    "express-rate-limit": "^6.5.1",
    "mongoose": "^6.5.1",
    "nodemon": "^2.0.19",
    "pm2": "^5.2.0"
  },
  "engines": {
    "node": ">=10.0.0"
  }
}
